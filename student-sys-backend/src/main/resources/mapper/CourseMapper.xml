<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haishili.studentsysbackend.mapper.CourseMapper">

    <resultMap id="CourseResultMap" type="com.haishili.studentsysbackend.pojo.entity.Course">
        <id column="id" property="id"/>
        <result column="course_name" property="courseName"/>
        <result column="teacher" property="teacher"/>
        <result column="max_capacity" property="maxCapacity"/>
        <result column="selected_count" property="selectedCount"/>
        <result column="point" property="point"/>
    </resultMap>

    <select id="getCourseById" resultMap="CourseResultMap">
        SELECT *
        FROM course
        WHERE id = #{id}
    </select>

    <update id="updateCourse">
        UPDATE course
        SET selected_count = #{selectedCount}
        WHERE id = #{id}
    </update>
</mapper>